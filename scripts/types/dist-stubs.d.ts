/* Loose stand-ins for tree-shaken helper modules that survive only as JSDoc
   references inside Astro's server bundle. Resolution-only: the runtime never
   imports them. */
declare module '*types.js';
